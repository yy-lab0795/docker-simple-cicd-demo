{
    "name": "web-container",
    "service": "web", // docker-compose.yml で設定したサービス名
    "workspaceFolder": "/workspace",
    "dockerComposeFile": "../../docker-compose.yml",
    "customizations": {
        "vscode": {
            // 拡張機能の管理, 拡張機能の詳細情報にある「識別子: Identifier」を使用する
            "extensions": [
                "firsttris.vscode-jest-runner"
            ]
        }
    }
}